#include "include/building.hpp"


using namespace std;

int main(int argc, char* argv[]){
    string name = argv[0];
    // string path = "./buildings/" + name + "/Gas.csv";
     
    exit(EXIT_SUCCESS);
}